# AGENTS.md

```yaml
version: v0.1
author: langya
update: 2026-05-19
```

## Project Snapshot

- `lexi` is a fresh macOS SwiftUI app generated by Xcode on 2026-05-19.
- Current code is still the default SwiftUI + Core Data template: a list of `Item` objects with one `timestamp` field.
- There is no app-specific product logic yet. When real requirements arrive, expect to replace the template scaffolding instead of extending it mechanically.
- This repository is an Xcode project, not a Swift Package. There is no `Package.swift`, package manager, test target, CI config, or lint config yet.

## Tech Stack

- Platform: macOS app only.
- Project: `lexi.xcodeproj`.
- Scheme: `lexi`.
- Source root: `lexi/`.
- Deployment target: macOS `26.4`.
- Swift version: `5.0`.
- Bundle identifier: `com.langya.lexi`.
- Core Data model: `lexi/lexi.xcdatamodeld`.

## Build And Run

Use this command for a basic Debug build:

```sh
xcodebuild -project lexi.xcodeproj -scheme lexi -configuration Debug build
```

Run the app from Xcode with the `lexi` scheme. There is no test target yet, so do not assume `xcodebuild test` is available until a test target is added.

## Current Files

- `lexi/lexiApp.swift`: app entry point; injects the Core Data view context into SwiftUI environment.
- `lexi/Persistence.swift`: `PersistenceController` wraps an `NSPersistentContainer` named `lexi`.
- `lexi/ContentView.swift`: template `NavigationView` list, add/delete toolbar actions, and `@FetchRequest` for `Item`.
- `lexi/lexi.xcdatamodeld`: Core Data model with generated `Item` class and `timestamp: Date?` attribute.
- `CLAUDE.md`: companion guidance with the same current project facts.

The Core Data container name in `Persistence.swift` must match the `.xcdatamodeld` filename.

## Coding Guidance

- Keep changes small and product-driven. Do not add abstractions before there is real app behavior to support.
- Treat the existing `ContentView`, `PersistenceController.preview`, and `Item` model as disposable template code unless the user explicitly wants to keep them.
- Do not carry template `fatalError` error handling into new production paths. Replace it with user-visible or recoverable handling when implementing real persistence flows.
- Prefer SwiftUI-native state and view composition before adding external dependencies.
- If adding dependencies, document why the built-in Apple framework is insufficient and how the dependency is integrated.
- Keep generated Xcode project edits narrow. Avoid unnecessary churn in `project.pbxproj`.

## Verification

- For compile-impacting changes, run the Debug build command above when feasible.
- For UI changes, build first, then use Xcode or the app UI for direct visual verification when needed.
- If tests are introduced later, update this file with the exact command and expected scope.
- If a command cannot be run because of local signing, SDK, or simulator constraints, report the exact blocker and any narrower validation completed.

## Safety

- Do not expose signing credentials, provisioning details, secrets, tokens, or local `.env` values in logs or responses.
- Do not add telemetry, analytics, network calls, or external services unless the user explicitly asks for them.
- Do not delete or reset user changes. This repository may contain uncommitted local work.
