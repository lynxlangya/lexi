# Lexi Sources

This directory is the new home for Lexi v1 source code. The old `ContentView.swift` and `Persistence.swift` template files have been removed; module ownership for the subdirectories follows `PR-PLAN.md`.
